import styled from 'styled-components';

const ButtonsContainer = styled.div`
  display: flex;
  justify-content: space-between;
`;

const LeftButtonsContent = styled.div``;

const RightButtonsContent = styled.div``;

export const Styled = { ButtonsContainer, LeftButtonsContent, RightButtonsContent };
